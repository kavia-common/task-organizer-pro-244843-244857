# Frontend (Next.js static export) environment variables
# NOTE: Because output is "export", API calls MUST use an absolute URL.
# Set this to the backend base URL (no trailing slash), e.g.:
# https://your-backend.example.com
NEXT_PUBLIC_API_BASE_URL=

# Optional: site URL for any future auth redirect links, etc.
NEXT_PUBLIC_SITE_URL=
